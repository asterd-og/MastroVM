ads wd "Hello World!"

mov regB ads
mov regD 0x3a2f

call print

.:
    jmp .

print:
    mov regC [regB]
    cmp regC 0
    je end
    mov [regD] regC
    add regD 1
    add regB 1
    jmp print

end:
    ret