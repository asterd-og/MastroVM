mov regA 0x707f
mov regB 0
mov regC 0

mov regD 0
mov regE 0

out regE 3

a:
in regB 1
cmp regB 0
jne b
cmp regB 0
je c
jmp a

b:
cmp regC 1
je a
mov [regA] regB
add regA 1
mov regB 0
mov regC 1
add regD 1
out 2 regD
jmp a

c:
mov regC 0
jmp a